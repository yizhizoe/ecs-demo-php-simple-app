version: 0.2

phases:
  install:
    commands:
      - pip install requests
      - pip install pytest
  build:
    commands:
      - pytest tests/ --junitxml /tmp/test_result.xml

artifacts:
  files: /tmp/test_result.xml
  discard-paths: yes
cache:
  paths: '/root/.cache/pip/**/*'